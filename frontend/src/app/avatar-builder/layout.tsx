'use client';

import SideNavbar from '@/components/SideNavbar';

export default function AvatarBuilderLayout({ children }: { children: React.ReactNode }) {
  return <SideNavbar>{children}</SideNavbar>;
}

